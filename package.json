{
  "name": "obsidian-project-manager-insights",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only assignee workload insights for the Obsidian Project Manager plugin.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit && npm test && npm run build",
    "test": "vitest run"
  },
  "keywords": [
    "obsidian",
    "project-manager",
    "workload",
    "assignee",
    "time-tracking"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "latest",
    "builtin-modules": "latest",
    "esbuild": "latest",
    "obsidian": "latest",
    "tslib": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}

